# Saved maps

This directory stores maps generated on the physical robot.

For the competition workflow, save a tested map from SLAM Toolbox / Nav2 as a YAML + PGM pair, for example:

```text
maps/arena.yaml
maps/arena.pgm
```

Maps used for competition should be committed to this repository so the exact tested environment file is versioned together with the robot configuration.
